<!-- #this is what gets rendered at /index -->
<h1>Images</h1>
<%= link_to "New Image", "/images/new" %>

<% @images.each do |image| %>
  <p><%= image.title %>, <%= image.artist %></p>
  <%= link_to image_tag(image.url), "/images/#{image.id}" %>
<% end %>
